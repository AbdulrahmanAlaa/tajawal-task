import { FilterhotelPipe } from './filterhotel.pipe';

describe('FilterhotelPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterhotelPipe();
    expect(pipe).toBeTruthy();
  });
});
